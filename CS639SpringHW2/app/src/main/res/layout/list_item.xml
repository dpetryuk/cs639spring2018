<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/itemLayout"
    android:layout_width="match_parent"
    android:layout_height="75dp"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/image"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_marginEnd="10dp"
        android:background="#999" />

    <TextView
        android:id="@+id/fact"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:textColor="#000"
        android:layout_toEndOf="@id/image"
        android:textAlignment="viewStart"
        android:layout_toStartOf="@id/nextFactBtn"
        android:visibility="invisible"
        />

    <Button
        android:id="@+id/nextFactBtn"
        android:layout_width="wrap_content"
        android:layout_height="35dp"
        android:layout_alignParentEnd="true"
        android:height="35dp"
        android:text="@string/next_fact"
        android:visibility="gone"
        android:focusableInTouchMode="false"
        android:focusable="false"
        android:textSize="10sp"/>

    <Button
        android:id="@+id/deleteFactBtn"
        android:layout_width="wrap_content"
        android:layout_height="35dp"
        android:layout_alignParentEnd="true"
        android:layout_alignStart="@+id/nextFactBtn"
        android:layout_below="@+id/nextFactBtn"
        android:text="@string/delete_fact"
        android:visibility="gone"
        android:focusableInTouchMode="false"
        android:focusable="false"
        android:textSize="10sp"/>

</RelativeLayout>